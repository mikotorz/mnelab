# CLAUDE.md

See @AGENTS.md for guidance on working with this repository.

## Agent skills

### Issue tracker

Issues and specs live as GitHub issues on `mikotorz/mnelab`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — `CONTEXT.md` and `docs/adr/` at the repo root (not yet created). See `docs/agents/domain.md`.
